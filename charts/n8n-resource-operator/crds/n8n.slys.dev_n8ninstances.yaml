---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: n8ninstances.n8n.slys.dev
spec:
  group: n8n.slys.dev
  names:
    kind: N8nInstance
    listKind: N8nInstanceList
    plural: n8ninstances
    shortNames:
    - n8ni
    - instance
    singular: n8ninstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.url
      name: URL
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .status.lastHealthCheck
      name: Last Check
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          N8nInstance is the Schema for the n8ninstances API
          It defines a connection to an n8n instance (cloud or self-hosted)
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: N8nInstanceSpec defines the desired state of N8nInstance
            properties:
              credentials:
                description: |-
                  Credentials references the secret containing the n8n API key
                  The secret must be in the same namespace as this N8nInstance
                properties:
                  secretKey:
                    default: api-key
                    description: SecretKey is the key in the secret containing the
                      API key
                    type: string
                  secretName:
                    description: |-
                      SecretName is the name of the secret containing the API key
                      The secret must be in the same namespace as the N8nInstance (operator namespace)
                    type: string
                required:
                - secretName
                type: object
              serviceRef:
                description: |-
                  ServiceRef references a Kubernetes service running n8n
                  Use this for self-hosted n8n within the same Kubernetes cluster
                  Either URL or ServiceRef must be specified, but not both
                properties:
                  name:
                    description: Name of the n8n service
                    type: string
                  namespace:
                    description: Namespace of the n8n service
                    type: string
                  port:
                    default: 5678
                    description: Port of the n8n service
                    type: integer
                required:
                - name
                - namespace
                type: object
              url:
                description: |-
                  URL is the full base URL of the n8n instance API
                  Use this for cloud-hosted n8n (e.g., "https://myorg.app.n8n.cloud")
                  or any externally accessible n8n instance
                  Either URL or ServiceRef must be specified, but not both
                type: string
            required:
            - credentials
            type: object
          status:
            description: N8nInstanceStatus defines the observed state of N8nInstance
            properties:
              conditions:
                description: Conditions of the n8n instance
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHealthCheck:
                description: LastHealthCheck is the last time the instance was successfully
                  health-checked
                format: date-time
                type: string
              observedGeneration:
                description: The generation observed by the controller
                format: int64
                type: integer
              ready:
                description: Ready indicates whether the n8n instance is reachable
                  and authenticated
                type: boolean
              url:
                description: URL is the resolved URL used to connect to the n8n instance
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
